@page "/Requests"

@* Eski rota — yeni CRUD sayfasına yönlendirir. *@

@code {
    [Inject] public NavigationManager Navigation { get; set; } = default!;

    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/Request", replace: true);
    }
}
